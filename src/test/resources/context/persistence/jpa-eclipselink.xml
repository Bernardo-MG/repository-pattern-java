<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:util="http://www.springframework.org/schema/util"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
        http://www.springframework.org/schema/util
        http://www.springframework.org/schema/util/spring-util-4.0.xsd">

    <!-- ================================== JPA ================================== -->

    <!-- JPA EntityManagerFactory -->
    <bean id="entityManagerFactory" class="${entityManagerFactory.class}">
        <property name="jpaVendorAdapter" ref="jpaAdapter" />
        <property name="dataSource" ref="dataSource" />
        <property name="persistenceUnitName" value="${jpa.persistenceUnitName}" />
        <property name="packagesToScan" value="${jpa.packagesToScan}" />
        <property name="jpaProperties" ref="jpaProperties" />
        <property name="loadTimeWeaver" ref="loadTimeWeaver" />
    </bean>

    <!-- Load time weaver -->
    <bean id="loadTimeWeaver" class="${loadTimeWeaver.class}" />

    <!-- JPA Adapter -->
    <bean id="jpaAdapter" class="${jpaAdapter.class}">
        <property name="database" value="${jpa.database}" />
        <property name="showSql" value="${jpa.showSql}" />
    </bean>

    <!-- Entity manager -->
    <bean id="entityManager" class="${entityManager.class}">
        <property name="entityManagerFactory" ref="entityManagerFactory" />
    </bean>

    <!-- JPA Properties -->
    <util:map id="jpaProperties">
        <entry key="eclipselink.cache.shared.default" value="${eclipselink.cache.shared.default}" />
        <entry key="eclipselink.weaving" value="${eclipselink.weaving}" />
        <entry key="eclipselink.ddl-generation" value="create-tables" />
        <entry key="eclipselink.ddl-generation.output-mode" value="database" />
    </util:map>

    <!-- Transaction manager -->
    <bean id="transactionManager" class="${transactionManager.class}">
        <property name="entityManagerFactory" ref="entityManagerFactory" />
    </bean>

    <!-- ============================== DATASOURCE =============================== -->

    <import resource="classpath:context/persistence/datasource.xml" />

</beans>